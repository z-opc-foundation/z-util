package com.zifang.util.expression.instruction;

/**
 * 指令注册器
 * 负责将操作码与对应的处理方法进行关联注册
 *
 * @author zifang
 * @version 1.0
 */
public class InstructionRegister {

    /**
     * 注册一个操作码与对应的处理引用
     *
     * @param opCode 操作码，标识指令类型
     * @param ref    处理方法的引用，用于执行对应的操作
     */
    public static void register(String opCode, Object ref) {

    }
}
